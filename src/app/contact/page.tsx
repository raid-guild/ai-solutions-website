import type { Metadata } from "next";
import ContactPage from "@/views/ContactPage";

export const metadata: Metadata = {
  title: "Contact | System Weaver",
  description:
    "Request a lightweight consultation for AI-native operations, workflow automation, and operated systems.",
};

export default ContactPage;
